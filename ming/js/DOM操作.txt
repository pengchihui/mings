DOM操作：增删改查

增：

createElement         创建元素节点
createTextNode        创建文本节点
a.appendChild(b)      把b添加到a里面
createAttribute       创建属性节点
setAttributeNode      设置属性节点

删：
a.removeChild(b)      在a里面删除b

改：
a.replaceChild(b,c)   在a里面用b替换掉c   （如果b是已存在的那么将会消失。）








查：
getElementById               通过id查找
getElementsByClassName        通过class查找（结果是数组）
getElementsByName            通过name查找（结果是数组）

querySelector       找到第一个匹配该选择器的元素
querySelectorAll


//通过关系来查找

parentElement       父级元素
children            子级元素
firstChild          第一个子节点（有可能是文本）
lastChild
parentNode          父级节点
childNodes          子级节点

firstElementChild   第一个标签子节点



cloneNode(true)

-----------------------------






document.title   获取页面标题


className      设置或读取class命名

ID              设置或读取id命名

parentNode
parentElement
nodeName
tagName

title

firstChild
lastChild

nextSibling

previousSibling

style

innerHTML         获取内容包括html
innerText



clientHeight      返回元素的可见宽度
clientWidth

offsetHeight       返回元素的宽度
offsetWidth

scrollTop          读取对象的滚动位置偏移量
scrollLeft

offsetTop          读取对象移动后的偏移量
offsetLeft


box.contains(dan)




clientWidth：返回元素的可见宽度，即clientWidth=width+padding，不包括滚动条和边框。
offsetWidth：返回元素的宽度，即offsetWidth=width+padding+scrollbar（滚动条）+border